<%- include('../partials/header', { title, busca:'', usuario:null, admin }) %>
<main class="container my-5" style="max-width: 500px;">
  <h1><%= title %></h1>
  <a href="/admin/categories" class="btn btn-secondary mb-3">Voltar</a>
  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>
  <form action="/admin/categories/new" method="POST">
    <div class="mb-3">
      <label class="form-label">Nome da Categoria</label>
      <input name="nome" class="form-control" placeholder="Ex: Música" required>
    </div>
    <button class="btn btn-primary">Criar Categoria</button>
  </form>
</main>
<%- include('../partials/footer') %>
